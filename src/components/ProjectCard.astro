---
const { title, description, link } = Astro.props;
---

<div class="c-card-light">
  <h3>{title}</h3>
  <p class="c-text-m">{description}</p>
  <a href={link} class="btn-main">View Project</a>
</div>

<style>
  h3 {
    font-size: 20px; /* From c-h3 */
    font-weight: 600;
    line-height: 1.3;
    color: #0f0f0f; /* color(60) */
    margin-bottom: 8px;
  }
</style>
